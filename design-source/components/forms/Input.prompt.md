One-line: the standard outlined field; pair it with `Field` for its label.

```jsx
<Input id="f-guests" type="number" min={1} placeholder="np. 120" />
```

2px ink border, 12px radius, cream fill. Focus state comes from `styles.css` (`--shadow-focus`), so link the stylesheet rather than restyling focus here.
